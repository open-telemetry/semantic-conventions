{% import 'requirement.j2' as requirement %}
{% import 'stability.j2' as stability %}
{% import 'entity_macros.j2' as entity %}
{% import 'notes.j2' as notes %}
{% import 'attribute_macros.j2' as attrs %}
{% import 'enum_macros.j2' as enums %}
{% import 'sampling_macros.j2' as sampling %}
{% import 'examples_macros.j2' as examples %}
{#- Macro for creating attribute table -#}
{% macro generate(signal, tag_filter, attribute_registry_base_url) %}{% if (tag_filter | length == 0) %}{% set filtered_attributes = signal.attributes | attribute_sort %}{% else %}{% set filtered_attributes = signal.attributes | selectattr("tag", "in", tag_filter) | attribute_sort %}{% endif %}

{% if (filtered_attributes is defined) and (filtered_attributes | length > 0) %}
{{ generate_any(signal, filtered_attributes, attribute_registry_base_url) | trim }}
{{ notes.render() }}{{ sampling.snippet(filtered_attributes, attribute_registry_base_url, signal.lineage.attributes) }}{{ enums.tables(filtered_attributes  | selectattr("type", "mapping"), notes) }}
{% endif %}
{% endmacro %}

{#- Macro for creating any attribute table -#}
{% macro generate_any(signal, filtered_attributes, attribute_registry_base_url) %}
{% if attribute_registry_base_url == "#" %}
{{ generate_attr_table(signal, filtered_attributes, attribute_registry_base_url) | trim }}
{% elif signal.type == "entity" or signal.type == "resource"%}
{{ entity.all_attribute_table(signal, filtered_attributes, attribute_registry_base_url) }}
{% else %}
{{ generate_general_table(signal, filtered_attributes, attribute_registry_base_url) | trim }}{% endif %}
{% endmacro %}

{#- Macro for creating attribute table -#}
{% macro generate_general_table(signal, filtered_attributes, attribute_registry_base_url) %}{% if (filtered_attributes is defined) and (filtered_attributes | length > 0) %}**Attributes:**

| Key | Stability | [Requirement Level](https://opentelemetry.io/docs/specs/semconv/general/attribute-requirement-level/) | Value Type | Description | Example Values |
|---|---|---|---|---|---|
{% for attribute in filtered_attributes %}| {{ attrs.name_with_link(attribute, attribute_registry_base_url, signal.lineage.attributes) }} | {{ stability.badge(attribute.stability, attribute.deprecated, attribute.brief) | trim }} | {{ requirement.render({"level": attribute.requirement_level, "name": attrs.name(attribute)}, notes) | trim }} | {{ attrs.type(attribute) }} | {{ attribute.brief | trim }}{{ notes.add({"note": attribute.note, "name": attrs.name(attribute)}) }} | {{ examples.format(attribute) | trim }} |
{% endfor %}
{% endif %}{% endmacro %}

{% macro generate_attr_table(signal, filtered_attributes, attribute_registry_base_url) %}**Attributes:**

| Key | Stability | Value Type | Description | Example Values |
|---|---|---|---|---|
{%- for attribute in filtered_attributes | sort(attribute="name") %}{% set attr_anchor = attribute.name | kebab_case %}
| <a id="{{ attr_anchor }}" href="#{{ attr_anchor }}">`{{ attrs.name(attribute) }}`</a> | {{ stability.badge(attribute.stability, attribute.deprecated, attribute.brief) | trim }} | {{ attrs.type(attribute) }} | {{ attribute.brief | trim }}{{ notes.add({"note": attribute.note, "name": attrs.name(attribute)}) }} | {{ examples.format(attribute) | trim }} |
{%- endfor %}
{% endmacro %}

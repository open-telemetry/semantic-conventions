groups:
  - id: url
    brief: Attributes describing URL.
    type: attribute_group
    prefix: url
    attributes:
      - ref: url.scheme
      - ref: url.full
      - ref: url.path
      - ref: url.query
      - ref: url.fragment

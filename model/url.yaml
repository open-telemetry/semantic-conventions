groups:
  - id: url
    brief: Attributes describing URL.
    type: attribute_group
    prefix: url
    attributes:
      - ref: url.scheme
      - ref: url.full
        tag: sensitive-information
      - ref: url.path
      - ref: url.query
        tag: sensitive-information
      - ref: url.fragment

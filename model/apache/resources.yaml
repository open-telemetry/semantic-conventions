groups:
  - id: resource.apache.server
    type: resource
    stability: development
    name: apache.server
    attributes:
      - ref: server.address
      - ref: server.port
    relationships:
      - is_a: server

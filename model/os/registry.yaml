groups:
  - id: registry.os
    type: attribute_group
    display_name: Operating System Attributes
    brief: >
        The operating system (OS) on which the process represented by this resource is running.
    note: >
        In case of virtualized environments, this is the operating system as it is observed by
        the process, i.e., the virtualized guest rather than the underlying host.
    attributes:
      - id: os.type
        type:
          members:
            - id: windows
              value: 'windows'
              brief: "Microsoft Windows"
              note: Used for the Windows NT Operating System.
              stability: development
            - id: linux
              value: 'linux'
              brief: "Linux"
              deprecated: 
                reason: renamed
                renamed_to: unix
              stability: development
            - id: unix
              value: 'unix'
              brief: "Unix"
              stability: development
            - id: darwin
              value: 'darwin'
              brief: "Apple Darwin"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: Darwin should be set as a `os.family` member instead.
              stability: development
            - id: freebsd
              value: 'freebsd'
              brief: "FreeBSD"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: FreeBSD should be set as the `os.name` instead.
              stability: development
            - id: netbsd
              value: 'netbsd'
              brief: "NetBSD"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: NetBSD should be set as the `os.name` instead.
              stability: development
            - id: openbsd
              value: 'openbsd'
              brief: "OpenBSD"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: OpenBSD should be set as the `os.name` instead.
              stability: development
            - id: dragonflybsd
              value: 'dragonflybsd'
              brief: "DragonFly BSD"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: DragonFly BSD should be set as the `os.name` instead.
              stability: development
            - id: hpux
              value: 'hpux'
              brief: "HP-UX (Hewlett Packard Unix)"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: HP-UX should be set as the `os.name` instead.
              stability: development
            - id: aix
              value: 'aix'
              brief: "AIX (Advanced Interactive eXecutive)"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: AIX should be set as the `os.name` instead.
              stability: development
            - id: solaris
              value: 'solaris'
              brief: "SunOS, Oracle Solaris"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: Solaris should be set as the `os.name` instead.
              stability: development
            - id: z_os
              value: 'z_os'
              brief: "Deprecated. Use `zos` instead."
              deprecated:
                reason: renamed
                renamed_to: unix
                note: zos should be set as the `os.name` instead.
              stability: development
            - id: zos
              value: 'zos'
              brief: "IBM z/OS"
              deprecated:
                reason: renamed
                renamed_to: unix
                note: zos should be set as the `os.name` instead.
              stability: development
            - id: jvm
              value: 'jvm'
              brief: "Java Virtual Machine"
              deprecated:
                reason: obsoleted
                note: Within OpenTelemetry `os.type` is used to describe the host OS rather than the runtime environment.
              stability: development
            - id: wasm
              value: 'wasm'
              brief: "WebAssembly System Interface"
              deprecated:
                reason: obsoleted
                note: Within OpenTelemetry `os.type` is used to describe the host OS rather than the runtime environment.
              stability: development
        brief: >
          The operating system type.
        stability: development
      - id: os.description
        type: string
        stability: development
        brief: >
          Human readable (not intended to be parsed) OS version information,
          like e.g. reported by `ver` or `lsb_release -a` commands.
        examples: ['Microsoft Windows [Version 10.0.18363.778]', 'Ubuntu 18.04.1 LTS']
      - id: os.name
        type: string
        stability: development
        brief: 'Human readable operating system name.'
        examples: ['iOS', 'Android', 'Ubuntu']
      - id: os.version
        type: string
        stability: development
        brief: >
          The version string of the operating system as defined in
          [Version Attributes](/docs/resource/README.md#version-attributes).
        examples: ['14.2.1', '18.04.1']
      - id: os.build_id
        type: string
        stability: development
        brief: 'Unique identifier for a particular build or compilation of the operating system.'
        examples: ['TQ3C.230805.001.B2', '20E247', '22621']

groups:
  - id: profile.frame
    type: attribute_group
    display_name: Frame types
    brief: >
      Describes the origin of a single frame in a Profile.
    attributes:
      - ref: profile.frame.type
        requirement_level: recommended
    annotations:
      docs:
        public_group: true

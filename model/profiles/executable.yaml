groups:
  - id: profile.build_id
    type: attribute_group
    brief: >
      Unique identifier for an executable.
    attributes:
      - ref: profile.build_id.gnu
        requirement_level: opt_in
      - ref: profile.build_id.go
        requirement_level: opt_in

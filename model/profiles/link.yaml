groups:
  - id: profile.link
    type: attribute_group
    brief: >
      Connect Profile information to Spans and Traces.
    attributes:
      - ref: profile.link.trace_id
        requirement_level: opt_in
      - ref: profile.link.span_id
        requirement_level: opt_in

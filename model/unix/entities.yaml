groups:
  - id: entity.unix.kernel
    type: entity
    stability: development
    name: unix.kernel
    brief: >
      The unix kernel on which the os represented by this resource is using.
    attributes:
      - ref: unix.kernel.name
        requirement_level: required
      - ref: unix.kernel.release
      - ref: unix.kernel.version

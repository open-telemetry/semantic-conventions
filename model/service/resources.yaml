groups:
  - id: resource.service
    type: resource
    name: service
    brief: >
      A service instance.
    stability: stable
    attributes:
      - ref: service.name
        requirement_level: required
      - ref: service.version
      - ref: service.namespace
      - ref: service.instance.id

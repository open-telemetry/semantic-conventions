groups:
  - id: peer
    type: attribute_group
    brief: "Operations that access some remote service."
    attributes:
      - ref: peer.service
        deprecated:
          reason: renamed
          renamed_to: service.peer.name

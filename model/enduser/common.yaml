groups:
  - id: identity
    type: attribute_group
    brief: >
      Describes end user identity.
    attributes:
      - ref: enduser.id
        requirement_level: recommended
      - ref: enduser.pseudo.id
        requirement_level: recommended

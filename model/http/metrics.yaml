file_format: definition/2

attribute_groups:
  - id: metric_attributes.http.server
    visibility: public
    stability: development
    brief: 'HTTP server metric attributes'
    attributes:
      - ref_group: attributes.http.server
      - ref: user_agent.synthetic.type
        requirement_level: opt_in

  - id: metric_attributes.http.client
    visibility: public
    stability: development
    brief: 'HTTP client metric attributes'
    attributes:
      - ref_group: attributes.http.client

  - id: metric_attributes.http.client.experimental
    visibility: public
    stability: development
    brief: 'HTTP client experimental metric attributes (extends metric_attributes.http.client)'
    attributes:
      - ref_group: attributes.http.client
      - ref: url.template
        requirement_level:
          conditionally_required: If available.
        note: >
          The `url.template` MUST have low cardinality. It is not usually available on HTTP clients, but may be known by the application or specialized HTTP instrumentation.

metrics:
  - name: http.server.request.duration
    stability: stable
    brief: "Duration of HTTP server requests."
    instrument: histogram
    unit: "s"
    annotations:
      code_generation:
        metric_value_type: double
    attributes:
      - ref_group: metric_attributes.http.server

  - name: http.server.active_requests
    stability: development
    brief: "Number of active HTTP server requests."
    instrument: updowncounter
    unit: "{request}"
    annotations:
      code_generation:
        metric_value_type: int
    attributes:
      - ref: http.request.method
        requirement_level: required
      - ref: url.scheme
        requirement_level: required
        examples: ["http", "https"]
      - ref: server.address
        requirement_level: opt_in
        brief: >
          Name of the local HTTP server that received the request.
        note: |
          See [Setting `server.address` and `server.port` attributes](/docs/http/http-spans.md#setting-serveraddress-and-serverport-attributes).

          > [!WARNING]
          > Since this attribute is based on HTTP headers, opting in to it may allow an attacker
          > to trigger cardinality limits, degrading the usefulness of the metric.
      - ref: server.port
        requirement_level: opt_in
        brief: >
          Port of the local HTTP server that received the request.
        note: |
          See [Setting `server.address` and `server.port` attributes](/docs/http/http-spans.md#setting-serveraddress-and-serverport-attributes).

          > [!WARNING]
          > Since this attribute is based on HTTP headers, opting in to it may allow an attacker
          > to trigger cardinality limits, degrading the usefulness of the metric.

  - name: http.server.request.body.size
    stability: development
    brief: "Size of HTTP server request bodies."
    instrument: histogram
    unit: "By"
    annotations:
      code_generation:
        metric_value_type: int
    note: >
      The size of the request payload body in bytes. This is the number of bytes transferred excluding headers and
      is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length)
      header. For requests using transport encoding, this should be the compressed size.
    attributes:
      - ref_group: metric_attributes.http.server

  - name: http.server.response.body.size
    stability: development
    brief: "Size of HTTP server response bodies."
    instrument: histogram
    unit: "By"
    annotations:
      code_generation:
        metric_value_type: int
    note: >
      The size of the response payload body in bytes. This is the number of bytes transferred excluding headers and
      is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length)
      header. For requests using transport encoding, this should be the compressed size.
    attributes:
      - ref_group: metric_attributes.http.server

  - name: http.client.request.duration
    stability: stable
    brief: "Duration of HTTP client requests."
    instrument: histogram
    unit: "s"
    annotations:
      code_generation:
        metric_value_type: double
    attributes:
      - ref_group: metric_attributes.http.client

  - name: http.client.request.body.size
    stability: development
    brief: "Size of HTTP client request bodies."
    instrument: histogram
    unit: "By"
    annotations:
      code_generation:
        metric_value_type: int
    note: >
      The size of the request payload body in bytes. This is the number of bytes transferred excluding headers and
      is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length)
      header. For requests using transport encoding, this should be the compressed size.
    attributes:
      - ref_group: metric_attributes.http.client.experimental

  - name: http.client.response.body.size
    stability: development
    brief: "Size of HTTP client response bodies."
    instrument: histogram
    unit: "By"
    annotations:
      code_generation:
        metric_value_type: int
    note: >
      The size of the response payload body in bytes. This is the number of bytes transferred excluding headers and
      is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length)
      header. For requests using transport encoding, this should be the compressed size.
    attributes:
      - ref_group: metric_attributes.http.client.experimental

  - name: http.client.open_connections
    stability: development
    brief: "Number of outbound HTTP connections that are currently active or idle on the client."
    instrument: updowncounter
    unit: "{connection}"
    annotations:
      code_generation:
        metric_value_type: int
    attributes:
      - ref: http.connection.state
        requirement_level: required
      - ref: network.peer.address
        requirement_level: opt_in
      - ref: network.protocol.version
        requirement_level: recommended
      - ref: server.address
        requirement_level: required
      - ref: server.port
        requirement_level: required
      - ref: url.scheme
        requirement_level: opt_in
        examples: ["http", "https"]

  - name: http.client.connection.duration
    stability: development
    brief: "The duration of the successfully established outbound HTTP connections."
    instrument: histogram
    unit: "s"
    annotations:
      code_generation:
        metric_value_type: double
    attributes:
      - ref: network.peer.address
        requirement_level: opt_in
      - ref: network.protocol.version
        requirement_level: recommended
      - ref: server.address
        requirement_level: required
      - ref: server.port
        requirement_level: required
      - ref: url.scheme
        requirement_level: opt_in
        examples: ["http", "https"]

  - name: http.client.active_requests
    stability: development
    brief: "Number of active HTTP requests."
    instrument: updowncounter
    unit: "{request}"
    annotations:
      code_generation:
        metric_value_type: int
    attributes:
      - ref: http.request.method
        requirement_level: recommended
      - ref: server.address
        requirement_level: required
        note: |
          In HTTP/1.1, when the [request target](https://www.rfc-editor.org/rfc/rfc9112.html#name-request-target)
          is passed in its [absolute-form](https://www.rfc-editor.org/rfc/rfc9112.html#section-3.2.2),
          the `server.address` SHOULD match the host component of the request target.

          In all other cases, `server.address` SHOULD match the host component of the
          `Host` header in HTTP/1.1 or the `:authority` pseudo-header in HTTP/2 and HTTP/3.
      - ref: server.port
        requirement_level: required
        note: |
          In the case of HTTP/1.1, when the [request target](https://www.rfc-editor.org/rfc/rfc9112.html#name-request-target)
          is passed in its [absolute-form](https://www.rfc-editor.org/rfc/rfc9112.html#section-3.2.2),
          the `server.port` SHOULD match the port component of the request target.

          In all other cases, `server.port` SHOULD match the port component of the
          `Host` header in HTTP/1.1 or the `:authority` pseudo-header in HTTP/2 and HTTP/3.
      - ref: url.scheme
        requirement_level: opt_in
        examples: ["http", "https"]
      - ref: url.template
        requirement_level:
          conditionally_required: If available.
        note: >
          The `url.template` MUST have low cardinality. It is not usually available on HTTP clients, but may be known by the application or specialized HTTP instrumentation.

groups:
  - id: entity.faas.function
    type: entity
    stability: development
    name: faas.function
    brief: >
      A serverless function.
    attributes:
      - ref: faas.function.id
        requirement_level: required
      - ref: faas.name
        requirement_level: required
      - ref: faas.version
      - ref: faas.instance
      - ref: faas.max_memory

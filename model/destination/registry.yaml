# yaml-language-server: $schema=https://raw.githubusercontent.com/open-telemetry/weaver/v0.25.1/schemas/semconv.schema.v2.json
file_format: definition/2
attributes:
  - key: destination.address
    type: string
    stability: development
    brief: "Destination address - domain name if available without reverse DNS lookup; otherwise, IP address or UNIX domain socket name."
    note: >
      `destination.address` SHOULD be the receiver address as observed at the point of instrumentation, for example the
      destination of the packet, flow, or exchange seen on the wire or socket. It SHOULD NOT be resolved to an address
      behind intermediaries such as proxies or load balancers.
    examples: ['destination.example.com', '10.1.2.80', '/tmp/my.sock']
  - key: destination.port
    type: int
    stability: development
    brief: 'Destination port number'
    examples: [3389, 2888]

groups:
  - id: faas_resource
    prefix: faas
    type: resource
    brief: >
      A serverless instance.
    attributes:
      - ref: faas.name
        requirement_level: required
      - ref: faas.version
      - ref: faas.instance
      - ref: faas.max_memory
      - ref: cloud.resource_id

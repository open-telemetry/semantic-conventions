change_type: enhancement

component: db

note: >
  Clarify `db.operation.batch.size` semantics for database batch operations, including
  multi-operand operations, parameterized operation batch APIs, and empty batches.

issues: [3811]

subtext:

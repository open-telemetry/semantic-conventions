change_type: enhancement
component: aws
note: Add AWS Bedrock AgentCore span and attributes.
issues: [2886]
subtext: |
  - Added `span.aws.bedrock.agentcore.client` extending `span.gen_ai.infrastructure.client`
  - AWS Bedrock AgentCore ARN attributes:
    - `aws.bedrock.agentcore.code_interpreter.arn`
    - `aws.bedrock.agentcore.browser.arn`
    - `aws.bedrock.agentcore.memory.arn`
    - `aws.bedrock.agentcore.runtime.arn`
    - `aws.bedrock.agentcore.runtime_endpoint.arn`
    - `aws.bedrock.agentcore.gateway.arn`
    - `aws.bedrock.agentcore.identity.workload_identity.arn`
  - AWS authentication credential provider ARN: `aws.auth.credential_provider.arn`
  - AWS gateway target ID: `aws.gateway.target.id`
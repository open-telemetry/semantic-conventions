change_type: breaking

component: messaging

note: |
  Support generic operations in messaging and rename metrics:

    - Deprecate `messaging.operation.type` in favor of now required `messaging.operation.name`.
    - Rename `messaging.publish.messages` to `messaging.client.produced.messages`
    - Rename `messaging.process.duration` to `messaging.client.process.duration`
    - Unify `messaging.publish.duration` and `messaging.receive.duration` into `messaging.client.operation.duration`
    - Unify `messaging.receive.messages` and `messaging.process.messages` into `messaging.client.consumed.messages`

issues: [1006, 947, 937]

change_type: enhancement
component: mobile
note: Rework `device.app.lifecycle` mobile event.
issues: [1880]
subtext: |
  The `device.app.lifecycle` event has been reworked to use attributes instead
  of event body fields. The `ios.state` and `android.state` attributes have
  been reintroduced to the attribute registry.


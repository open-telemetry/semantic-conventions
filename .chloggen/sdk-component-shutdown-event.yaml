change_type: enhancement

component: otel

note: >
  Add `otel.sdk.component.shutdown` self-observability event, the supporting
  `otel.component.shutdown.duration` attribute, and `tracer_provider`,
  `logger_provider`, and `meter_provider` values for `otel.component.type`.

issues: [3718]

subtext:

change_type: enhancement

component: otel

note: Add `otel.sdk.component.shutdown` self-observability event and supporting attributes (`otel.component.shutdown.outcome`, `otel.component.dropped_count`).

issues: [3718]

subtext:

# GenAI Memory Stories - Environment Variables (Sample)
# Copy to `.env.local` (or pass `--env-file` to `run_and_view.py`) and fill in credentials.
#
# Notes:
# - Never commit real keys (especially `.env.local`).
# - `GENAI_MEMORY_CAPTURE_CONTENT` is opt-in and MUST remain false by default.

# =============================================================================
# Demo Runner / Viewer
# =============================================================================
OTEL_SERVICE_NAME=genai-memory-stories

# Exporter selection (comma-separated): console,appinsights,otlp,jaeger,laminar,langfuse,traceloop
GENAI_MEMORY_EXPORTERS=appinsights
GENAI_MEMORY_USE_SIMPLE_PROCESSOR=true

# Opt-in content capture (sensitive!)
GENAI_MEMORY_CAPTURE_CONTENT=false

# =============================================================================
# OpenAI (real LLM calls in demos)
# =============================================================================
OPENAI_API_KEY=
DEMO_LLM_MODE=auto            # auto|openai|mock
DEMO_OPENAI_MODEL=gpt-4.1
OPENAI_BASE_URL=             # e.g. https://<your_openai_compatible_endpoint>/v1
DEMO_OPENAI_TIMEOUT_SECONDS=30

# =============================================================================
# Azure Application Insights - Trace Export
# =============================================================================
# Azure Portal > Application Insights > Overview > Connection String
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=<INSTRUMENTATION_KEY>;IngestionEndpoint=<INGESTION_ENDPOINT>

# =============================================================================
# Azure Application Insights - Trace Viewer Queries (Entra ID / RBAC)
# =============================================================================
# Requires: `az login` and RBAC access (e.g., Monitoring Reader).
# Azure Portal > Application Insights > Overview > Resource ID
APPINSIGHTS_RESOURCE_ID=/subscriptions/YOUR_SUB/resourceGroups/YOUR_RG/providers/Microsoft.Insights/components/YOUR_APPINSIGHTS_NAME

# Optional alternative (name + resource group) for `az monitor app-insights query`
# APPINSIGHTS_APP=YOUR_APPINSIGHTS_NAME
# APPINSIGHTS_RESOURCE_GROUP=YOUR_RG

# Optional legacy mode (only if your org still allows API keys)
# APPINSIGHTS_APP_ID=your-app-insights-application-id
# APPINSIGHTS_API_KEY=your-app-insights-api-key

# =============================================================================
# Optional backends supported by `genai_memory_otel.exporters`
# =============================================================================

# Laminar (LMNR)
LMNR_PROJECT_API_KEY=

# Langfuse
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=            # e.g. https://<your_langfuse_host>

# Traceloop (OpenLLMetry)
TRACELOOP_API_KEY=
